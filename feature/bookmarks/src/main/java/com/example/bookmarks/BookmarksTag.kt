package com.example.bookmarks

import org.jetbrains.annotations.TestOnly

@TestOnly
object BookmarksTag {
    object Bar {
        const val BOOKMARK_BAR = "bookmark_bar"
    }
    object Content {
        const val BOOKMARK_COLUMN = "bookmark_column"
    }
}